{% extends 'base.html.twig' %}

{% block title %}Training{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('trainer') }}
{% endblock %}

{% block body %}

    <div id="app">
        <Trainer
            model-id="{{ modelId }}"
            load-model-url="{{ path('app_configurator_loadmodel') }}"
            configurator-url="{{ path('app_configurator', {id: modelId}) }}"
        ></Trainer>
    </div>

{% endblock %}
